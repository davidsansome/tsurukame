#import <UIKit/UIKit.h>

void RunSuccessAnimation(UIView *button,
                         UIView *doneLabel,
                         bool isSubjectFinished,
                         bool didLevelUp,
                         int newSrsLevel);
